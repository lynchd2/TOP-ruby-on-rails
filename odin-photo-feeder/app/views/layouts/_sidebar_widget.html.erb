<div class="flickr">
  <ul>
    <% photos.each do |photo| -%>
	<% info = flickr.photos.getInfo(photo_id: photo.id) %>
	    <%= link_to(image_tag(FlickRaw.url_b(info), :class => 'flickr_photo', title: photo.title), FlickRaw.url_photopage(info)) %>
    <% end %>
  </ul>
</div>